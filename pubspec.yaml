name: capstone6
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.5
  http: ^1.2.1
  fl_chart: ^1.2.0
  shared_preferences: ^2.3.2

  # ── Supabase Auth (menggantikan Firebase Auth + google_sign_in) ──
  supabase_flutter: ^2.8.4

  # ── Deteksi wajah & emosi real-time ──
  camera: ^0.11.0
  google_mlkit_face_detection: 0.12.0
  google_mlkit_commons: ^0.9.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/google_logo.png

  
